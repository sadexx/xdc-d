export enum EPaymentCaptureStrategy {
  SAME_COMPANY_COMMISSION = "same-company-commission",
  CORPORATE_CAPTURE = "corporate-capture",
  INDIVIDUAL_CAPTURE = "individual-capture",
  VALIDATION_FAILED = "validation-failed",
}
